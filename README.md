# Hardsub Encoder GUI

Графическая оболочка (GUI) для массового кодирования видео с использованием FFmpeg, ориентированная на аппаратное ускорение NVIDIA (NVENC). 
Предназначена для упрощения процесса хардсаба (вшивания субтитров) и приведения видео к единому стандарту (HEVC/H.265).

---

## 🚀 Основные возможности

*   **Аппаратное кодирование (GPU)**: Использование NVIDIA NVENC (HEVC/H.265) для высокой скорости.
*   **Программное кодирование (CPU)**: Резервный режим x265, если GPU недоступен.
*   **Режим Lossless**: Быстрое переключение на настройки кодирования без потерь.
*   **Автоматическое вшивание субтитров**: Поиск файлов `.ass` (или `.srt` и др., поддерживаемых libass) и шрифтов `.ttf`/`.otf` рядом с видео или в контейнере MKV и их "прожиг" (Hardsub).
*   **Обработка аудио**: Гибкая настройка кодеков (AAC, AC3, FLAC, Copy), битрейта и каналов.
*   **Пакетная обработка**: Очередь файлов, Drag & Drop.
*   **Информативные уведомления**: Звуковые сигналы и уведомления в трее по завершении работы.

---

## ⚙️ Системные требования

1.  **ОС**: Windows 10/11 (x64).
2.  **Видеокарта**: NVIDIA GeForce (проверено на сериях 10xx и новее) с поддержкой NVENC.
    *   *Драйверы должны быть установлены и обновлены.*
    *   Утилита `nvidia-smi` желательна, но не критична (программа распознает GPU и через FFmpeg).
3.  **FFmpeg**: Должен быть установлен в системе (в PATH) или находиться рядом с исполняемым файлом программы (`ffmpeg.exe`, `ffprobe.exe`).

---

## 🛠️ Установка и Запуск

1.  Скачайте релиз или склонируйте репозиторий.
2.  Убедитесь, что `ffmpeg.exe` доступен.
3.  Запустите `main.py` (требуется Python 3.10+ и зависимости из `requirements.txt`) или скомпилированный `.exe`.

При запуске программа автоматически проверит:
*   Наличие FFmpeg/FFprobe.
*   Наличие NVIDIA GPU и драйверов.
*   Поддержку энкодера `hevc_nvenc`.
*   Поддержку фильтров субтитров.

> **Важно**: Если GPU не найден или не поддерживает NVENC, программа *автоматически* переключится в режим CPU (x265) и заблокирует настройки GPU, чтобы избежать ошибок.

---

## 📖 Руководство пользователя

Интерфейс разделен на вкладки для удобной настройки. При наведении курсора на любой элемент управления появляется всплывающая подсказка с пояснением.

### 1. Вкладка "Файлы и назначение"

*   **Список файлов**: 
    *   Перетащите файлы в окно (поддерживаются `.mp4`, `.mkv`, `.avi` и др.) или используйте кнопку **"Выбрать видеофайлы"**.
    *   В центре пустого списка есть подсказка для Drag & Drop.
*   **Параметры вывода**:
    *   **Папка вывода**: По умолчанию создается папка `ENCODED_HEVC_NVIDIA_GUI` рядом с программой. Можно выбрать любую другую.
    *   **Использовать исходный путь**: Если включено, файлы будут сохраняться в ту же папку, где лежит исходное видео (в подпапку вывода).

### 2. Вкладка "Видео"

Здесь выбирается метод сжатия видеопотока.

#### Общие настройки:
*   **Режим Lossless (Без потерь)**:
    *   Чекбокс для мгновенного переключения в режим максимального качества.
    *   **GPU**: Включает режим `constqp` (Constant Quantization Parameter) с `QP=0` и Tuning `lossless`.
    *   **CPU**: Включает режим `CRF` со значением `0`.
    *   *При активации детальные настройки битрейта блокируются.*

#### NVIDIA NVENC (GPU) - Основной режим:
*   **Пресет**: Скорость кодирования. По умолчанию `p7` (самое медленное/качественное).
*   **Управление битрейтом**:
    *   `vbr_hq` (Variable Bitrate High Quality) - по умолчанию.
    *   `cbr` (Constant Bitrate).
    *   `constqp` (Постоянный квантователь - качество не зависит от битрейта).
*   **Битрейт**: Целевой битрейт в Мбит/с (по умолчанию 4 Мбит/с).
*   **QP**: Параметр качества для режима `constqp` (0-51, где 0 - лучшее).
*   **Tuning**: `hq` (High Quality), `ll` (Low Latency), `lossless`.
*   **Lookahead**: Предварительный анализ кадров. Позволяет задать количество кадров (0-32). Включение улучшает качество, но потребляет больше памяти GPU.
*   **Spatial AQ**: Пространственное адаптивное квантование (включено по умолчанию).
*   **10-бит (Main10)**: Принудительное включение 10-битного профиля HEVC.

#### CPU (x265) - Резервный режим:
*   **Пресет**: От `ultrafast` до `veryslow`. По умолчанию `medium`.
*   **Режим**:
    *   **CRF** (Constant Rate Factor): Управление качеством (по умолчанию 23). Меньше = лучше.
    *   **Битрейт**: Целевой средний битрейт.

#### Кадрирование и Разрешение:
*   **Автоматически обрезать черные полосы**: Анализирует видео фильтром `cropdetect` и удаляет черные поля (letterboxing). Выключено по умолчанию.
*   **Принудительное разрешение**: Позволяет изменить размер видео (скейлинг).

### 3. Вкладка "Аудио"

Настройки обработки звуковых дорожек.

*   **Кодек**:
    *   `aac`, `ac3`, `libopus`, `mp3` - с перекодированием.
    *   `flac` - сжатие без потерь.
    *   `copy` - копирование исходной дорожки без изменений (битрейт и каналы игнорируются).
*   **Битрейт**: 96k - 320k (по умолчанию 256k). Игнорируется для `copy` и `flac`.
*   **Каналы**: Стерео / Моно / Исходные.
*   **Метаданные**:
    *   **Название**: Заголовок дорожки в файле (по умолчанию "Русский [Дубляжная]").
    *   **Язык**: Код языка ISO 639-2 (по умолчанию `rus`).

### 4. Вкладка "Субтитры"

Автоматическое вшивание субтитров (Hardsub). Программа ищет файл субтитров с тем же именем, что и видеофайл.

*   **Не вшивать надписи**: Полностью отключает обработку субтитров.
*   **Удалить ТБ "ТО Дубляжная"**: Фильтрует строки субтитров, содержащие специфическую техническую информацию дабберов (основано на тегах ASS/тексте).

### 5. Вкладка "Запуск"

*   **Прогресс**: Отображает статус текущего файла и общей очереди.
*   **Лог**: Внизу окна отображается подробный журнал действий.
*   **Кнопка "Начать кодирование"**: Запускает процесс. Превращается в "Остановить" во время работы.

---

## 🔔 Поведение при завершении

После завершения обработки очереди (или при ошибке):
1.  Воспроизводится системный звуковой сигнал (Beep).
2.  Показывается всплывающее уведомление в системном трее Windows.
3.  Кнопка запуска становится снова активной.

---

## 🔧 Структура файлов

*   `main.py`: Точка входа.
*   `src/app_config.py`: Основные константы и настройки по умолчанию.
*   `src/ui/`: Логика графического интерфейса.
*   `src/ffmpeg/`: Модули для работы с FFmpeg (генерация команд, детекция оборудования).
*   `src/encoding/`: Воркеры для асинхронного выполнения задач кодирования.
*   `fonts/`: Папка для пользовательских шрифтов (если требуются для субтитров).